{
  "name": "geartrade-workspace",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "nullshot dev",
    "build": "npm run build --workspaces",
    "install:mcp": "nullshot install",
    "validate": "nullshot validate",
    "list": "nullshot list",
    "terminal": "bash scripts/start-terminal.sh",
    "terminal:build": "cd packages/terminal-ui && pnpm install && pnpm run build"
  },
  "devDependencies": {
    "@nullshot/cli": "latest",
    "typescript": "^5.0.0"
  }
}