import { DryRunManager } from "../utils/dry-run.js";
export interface TemplateConfig {
    mcp: {
        repositoryUrl: string;
        templateName: string;
    };
    agent: {
        repositoryUrl: string;
        templateName: string;
    };
}
export declare class TemplateManager {
    private git;
    private dryRunManager;
    constructor(dryRunManager: DryRunManager);
    private initGit;
    private static readonly TEMPLATE_CONFIG;
    /**
     * Create a new project from a template
     */
    createProject(templateType: "mcp" | "agent", projectName: string, targetDirectory: string): Promise<void>;
    /**
     * Validate that the target directory doesn't exist
     */
    private validateTargetDirectory;
    /**
     * Validate that the parent directory exists and is writable
     */
    private validateParentDirectory;
    /**
     * Clone the git repository to the target directory
     */
    private cloneRepository;
    /**
     * Update package.json and wrangler.jsonc with the new project name
     */
    private updateProjectFiles;
    /**
     * Update package.json with the new project name
     */
    private updatePackageJson;
    /**
     * Update wrangler.jsonc with the new project name
     */
    private updateWranglerConfig;
    /**
     * Remove the .git directory to start fresh
     */
    private cleanupGitHistory;
    /**
     * Get the list of available template types
     */
    static getAvailableTemplates(): Array<{
        type: "mcp" | "agent";
        name: string;
        url: string;
    }>;
}
//# sourceMappingURL=template-manager.d.ts.map