export interface ProjectConfig {
    projectName: string;
    targetDirectory: string;
    fullPath: string;
}
export declare class InputManager {
    /**
     * Prompt user for project configuration using prompts library
     */
    promptForProjectConfig(templateType: "mcp" | "agent"): Promise<ProjectConfig>;
    /**
     * Generate a random example directory name
     */
    private generateExampleName;
    /**
     * Generate a project name based on the target directory
     */
    private generateProjectName;
    /**
     * Sanitize a string to be a valid npm package name
     */
    private sanitizeProjectName;
    /**
     * Validate if a path is safe and valid
     */
    private isValidPath;
}
//# sourceMappingURL=input-manager.d.ts.map