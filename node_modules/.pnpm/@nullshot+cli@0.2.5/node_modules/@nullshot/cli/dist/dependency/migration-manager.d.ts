import { DryRunManager } from "../utils/dry-run.js";
export interface MigrationResult {
    dependency: string;
    wranglerConfigPath: string;
    success: boolean;
    error?: string;
}
export declare class MigrationManager {
    private dryRunManager?;
    constructor(dryRunManager?: DryRunManager | undefined);
    /**
     * Get all dependency wrangler config paths for use with wrangler -c flags
     */
    getDependencyWranglerFlags(dependencyWranglerPaths: Array<{
        name: string;
        wranglerConfigPath: string;
    }>): string[];
    /**
     * Execute D1 migrations for dependencies with D1 databases
     */
    executeD1MigrationsForDependencies(dependencies: Array<{
        name: string;
        wranglerConfigPath: string;
        d1Databases?: string[];
    }>): Promise<MigrationResult[]>;
    /**
     * Execute D1 migration for a single database binding
     */
    private executeD1MigrationForBinding;
    /**
     * Run wrangler D1 migrations using the dependency's configuration
     */
    private runWranglerD1Migrations;
}
//# sourceMappingURL=migration-manager.d.ts.map